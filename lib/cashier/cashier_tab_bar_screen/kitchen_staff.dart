import 'package:flutter/material.dart';

class KitchenStaff extends StatelessWidget {
  const KitchenStaff({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
